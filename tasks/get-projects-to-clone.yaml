apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: get-projects-to-clone
spec:
  params:
    - name: isServerList
      default: []
      description: Aray of IntegrationServer yamls
      type: array
    - name: config-repo-dirname
      type: string
  results:
    - name: projects
      type: array
    - name: integrationServerNames
      type: array
    - name: isProjects
      type: array
  steps:
    - name: parse-source-properties
      image: image-registry.openshift-image-registry.svc:5000/vadym/sh-tools
      args:
        - $(params.isServerList[*])
      script: |
        #!/usr/bin/env bash
        set -ex

        bash --version

        allintegrationservers=()
        integrationServers=()
        allProjects=()
        isProjects=()

        function serialize_array() {
          declare -n _array="${1}" _str="${2}" # _array, _str => local reference vars
          local IFS="${3:-$'\x01'}"
          # shellcheck disable=SC2034 # Reference vars assumed used by caller
          _str="${_array[*]}" # * => join on IFS
        }

        function deserialize_array() {
          IFS="${3:-$'\x01'}" read -r -a "${2}" <<<"${!1}" # -a => split on IFS
        }

        extract_all_projects() {
          yaml_file=$1
          new_projects=$(yq e '.metadata.annotations.ab-bars[].projects[]' "$yaml_file" 2>/dev/null)
          
          echo "==========================="
          newProjects=($(yq eval '.metadata.annotations.ab-bars[].projects[]' "$yaml_file" | tr '\n' ',' | sed 's/,$//'))
          echo -n $newProjects
          echo "==========================="

          if [ -n "$new_projects" ]; then
              # Add the extracted projects to the array variable
              allProjects+=($new_projects)
          fi
        }

        for file in $@; do
          integration_server_yaml_path="$(workspaces.input.path)/$(params.config-repo-dirname)/$file"
          extract_all_projects $integration_server_yaml_path

          imageName=$(yq e '.metadata.annotations.ab-image-name' "$yaml_file" 2>/dev/null)

          integrationServerName=$(yq e '.metadata.name' "$yaml_file" 2>/dev/null)
          allintegrationservers+=($integrationServerName)

          # get bars for for specific yaml file
          bars=($(yq e '.metadata.annotations.ab-bars[].bar-name' "$yaml_file" 2>/dev/null))
          
          declare -a allBars

          # loop over bars
          for i in "${!bars[@]}"; do
            projects=($(yq e ".metadata.annotations.ab-bars[$i].projects | join(\";\")" "./is.yaml" 2>/dev/null))
            allBars["$i"]=${projects[@]}
          done

          serialize_array allBars serialized_data '|'
          integrationServers["$s"]=$serialized_data
        done

        writeJSONArray() {
          local result_path=$1
          shift
          local array=( "$@" )

          # Build a JSON array manually
          jsonArray="["
          for item in "${array[@]}"; do
              jsonArray+="\"$item\","
          done
          jsonArray="${jsonArray%,}"  # Remove the trailing comma
          jsonArray+="]"

          echo -n $jsonArray > $result_path
        }

        writeJSONArray $(results.projects.path) "${projects[@]}"

        writeJSONArray $(results.integrationServerNames.path) "${allintegrationservers[@]}"

        writeJSONArray $(results.isProjects.path) "${integrationServers[@]}"
  workspaces:
    - name: input
